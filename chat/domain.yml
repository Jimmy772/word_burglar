version: "3.1"

intents:
  - query_books

entities:
  - average_rating
  - publication_year  
  - publication_month
  - description

responses:
actions:
  - action_query_database


session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
